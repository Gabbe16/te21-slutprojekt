{% extends "layout.njk" %}
{% block content %}

<main class="wrapper">
    <div class="card">
        <h1> {{ title }} </h1>
        <h2>Här finns alla recensioner som är på databasen.</h2>
        <h2>Klicka in på den recension som du vill läsa.</h2>
        <p>För att skapa en recension logga in eller skapa ett konto!</p>
        <ul class="reviewlist">
            {% for review in reviews %}
            <li><a href="/reviews/{{review.id}}">{{ review.title }}</a></li>
            {% endfor %}
        </ul>
    </div>
</main>

{% endblock %}