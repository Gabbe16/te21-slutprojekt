{% extends "layout.njk" %}
{% block content %}

<main class="wrapper">
    <div class="card">
        <h1> {{ review.title }}</h1>
        <h2> Score: {{ review.score }}</h2>
        <p> {{ review.text }}</p>

        <footer>
            <h3> Skrivet av {{ user }}</h3>
        </footer>
    </div>
</main>

{% endblock %}